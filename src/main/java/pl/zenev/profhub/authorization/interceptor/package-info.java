/**
 * Interceptors implementations. Interceptors intercept other methods so no two different logics are put into single
 * method. For example transaction or authorization is not put into the same method as business logic flow.
 */
package pl.zenev.profhub.authorization.interceptor;
